每次任务前告知用户复杂度如何
- 若低复杂度则为了效率不走superpowers流程,我会手动切换到plan-only mode, 这是codex(你)自己的计划模式

- 若中高复杂度就依然走superpowers的流程

- 还有 不用重复读那个using-superpowers, 多浪费token啊

! 不要一开始就给ai看, 要至少进行一轮, 而且一句一句给 不要一次全给

- 在开始落spec/plan之前  看情况增加一个搜集信息环节


结束旧会话开新会话之前, 做一个防踩坑