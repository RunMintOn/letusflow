# 对齐--严格执行

每次任务前告知用户复杂度如何

```
- 若低复杂度则为了效率不走superpowers流程,我会手动切换到plan-only mode, 这是codex(你)自己的计划模式
- 若中复杂度就依然走superpowers的流程, 但是可以跳过spec
- 若高复杂度建议按完整流程
- 你就简洁直接回答我
```

- 我已经切换到 codex 的 plan mode

- 还有 不用重复读那个using-superpowers, 多浪费token啊

! 不要一开始就给ai看, 要至少进行一轮, 而且一句一句给 不要一次全给

- 在开始落spec/plan之前  看情况增加一个搜集信息环节  或者对于比较重要的 , 记得"如果很复杂 有没有别的更精妙更优雅的解法?" 让ai给出多种方案

- 那就直接进入plan mode
- 在此之前 你是否已经搜集了足够的必要信息?

- 结束旧会话开新会话之前, 做一个防踩坑

# 优化ui

可以参考那些 比如excalidraw 插件的设计

~~比如 直接默认打开 就不要搞多按ctrl P这一步了~~

比如 背景, 点子背景 深色模式

- 还有 右键拖动 

- feat 语法拓展

- 增加一个开启 关闭拖拽 的功能

- 完善语法 和 报错机制 对 ai友好

- bug: 删除线段之后节点也不见

自动发送: 就在main, 在本次对话执行

目前你先不要改动，你先跟我对齐一下，你明白我说的意思吗？

- 一定要先计划再开始 
- 前端相关的最少也要 先plan

--------------
--------------

